{
  "name": "local-ai-preflight-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Local AI CLI preflight gate with a minimal TypeScript core.",
  "bin": {
    "local-ai-preflight": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.js",
    "verify": "npm run build && node --test tests/*.test.js && node dist/cli.js --help >/dev/null && node dist/cli.js --version >/dev/null"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
